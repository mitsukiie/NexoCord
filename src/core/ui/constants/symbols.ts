/** Internal metadata key used to track container attachments. */
export const ATTACHMENTS = Symbol('ui.display.attachments');

/** Internal metadata key used to mark rendered component arrays. */
export const RENDERED = Symbol('ui.display.rendered');

/** Internal metadata key used to store rendered attachments. */
export const RENDERED_ATTACHMENTS = Symbol('ui.display.rendered.attachments');
