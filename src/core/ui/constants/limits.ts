/** Maximum number of components allowed in a rendered message. */
export const MAX_MESSAGE_COMPONENTS = 40;

/** Maximum number of characters allowed in a display text block. */
export const MAX_TEXT_LENGTH = 4000;

/** Maximum number of components allowed inside an action row. */
export const MAX_ROW_COMPONENTS = 5;

/** Maximum number of options allowed in a string select menu. */
export const MAX_STRING_SELECT_OPTIONS = 25;

/** Maximum length of a select placeholder. */
export const MAX_SELECT_PLACEHOLDER_LENGTH = 150;

/** Maximum number of text blocks allowed inside a section. */
export const MAX_SECTION_TEXTS = 3;

/** Maximum number of items allowed inside a gallery. */
export const MAX_GALLERY_ITEMS = 10;

/** Maximum length of a button label. */
export const MAX_BUTTON_LABEL_LENGTH = 80;

/** Maximum length of a button custom id. */
export const MAX_BUTTON_CUSTOM_ID_LENGTH = 100;
